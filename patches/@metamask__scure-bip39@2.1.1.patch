diff --git a/dist/index.d.ts b/dist/index.d.ts
index 5275d67b158ead15a4af240fd2d1ab65af5f285e..60db13a50cfc12f39e0e03e95263c2534f79f48b 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -50,7 +50,7 @@ export declare function validateMnemonic(mnemonic: string | Uint8Array, wordlist
  * await mnemonicToSeed(mnem, 'password');
  * // new Uint8Array([...64 bytes])
  */
-export declare function mnemonicToSeed(mnemonic: string | Uint8Array, wordlist: string[], passphrase?: string): Promise<Uint8Array>;
+export declare function mnemonicToSeed(mnemonic: Uint8Array, wordlist: string[], passphrase?: string): Promise<Uint8Array>;
 /**
  * Irreversible: Uses KDF to derive 64 bytes of key data from mnemonic + optional password.
  * @param mnemonic 12-24 words (string | Uint8Array)
@@ -62,4 +62,4 @@ export declare function mnemonicToSeed(mnemonic: string | Uint8Array, wordlist:
  * mnemonicToSeedSync(mnem, 'password');
  * // new Uint8Array([...64 bytes])
  */
-export declare function mnemonicToSeedSync(mnemonic: string | Uint8Array, wordlist: string[], passphrase?: string): Uint8Array;
+export declare function mnemonicToSeedSync(mnemonic: Uint8Array, wordlist: string[], passphrase?: string): Uint8Array;
diff --git a/dist/index.js b/dist/index.js
index e0c500c44b31f83a5f043c78805be1f7accbef08..afab9e4d13d8352803f78a0112547f098fc8c75e 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -131,8 +131,8 @@ const salt = (passphrase) => nfkd(`mnemonic${passphrase}`);
  * // new Uint8Array([...64 bytes])
  */
 function mnemonicToSeed(mnemonic, wordlist, passphrase = '') {
-    const encodedMnemonicUint8Array = encodeMnemonicForSeedDerivation(mnemonic, wordlist);
-    return (0, pbkdf2_1.pbkdf2Async)(sha512_1.sha512, encodedMnemonicUint8Array, salt(passphrase), { c: 2048, dkLen: 64 });
+    // const encodedMnemonicUint8Array = encodeMnemonicForSeedDerivation(mnemonic, wordlist);
+    return (0, pbkdf2_1.pbkdf2Async)(sha512_1.sha512, mnemonic, salt(passphrase), { c: 2048, dkLen: 64 });
 }
 exports.mnemonicToSeed = mnemonicToSeed;
 /**
@@ -147,8 +147,8 @@ exports.mnemonicToSeed = mnemonicToSeed;
  * // new Uint8Array([...64 bytes])
  */
 function mnemonicToSeedSync(mnemonic, wordlist, passphrase = '') {
-    const encodedMnemonicUint8Array = encodeMnemonicForSeedDerivation(mnemonic, wordlist);
-    return (0, pbkdf2_1.pbkdf2)(sha512_1.sha512, encodedMnemonicUint8Array, salt(passphrase), { c: 2048, dkLen: 64 });
+    // const encodedMnemonicUint8Array = encodeMnemonicForSeedDerivation(mnemonic, wordlist);
+    return (0, pbkdf2_1.pbkdf2)(sha512_1.sha512, mnemonic, salt(passphrase), { c: 2048, dkLen: 64 });
 }
 exports.mnemonicToSeedSync = mnemonicToSeedSync;
 /**
